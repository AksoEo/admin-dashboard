// TODO
export const FIELDS = {
    title: {
        component ({ value }) {
            return value;
        },
    },
    description: {
        component ({ value }) {
            return value;
        },
    },
    timeFrom: {
        component ({ value }) {
            return value;
        },
    },
    location: {
        component ({ value }) {
            return value;
        },
    },
};
