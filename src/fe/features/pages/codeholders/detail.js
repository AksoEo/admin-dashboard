import { h } from 'preact';
import Page from '../../../components/page';
import DetailView from '../../../components/detail';

export default class Detail extends Page {
    render () {
        return <DetailView locale={{}} />;
    }
}
