export const FIELDS = {
    type: {
        component ({ value }) {
            return value;
        },
        weight: 0.25,
    },
    name: {
        component ({ value }) {
            return value;
        },
    },
    description: {
        component ({ value }) {
            return value;
        },
        weight: 2,
    },
};
