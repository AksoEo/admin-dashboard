export const FIELDS = {
    name: {
        component ({ value }) {
            return value;
        },
    },
    description: {
        component ({ value }) {
            return value;
        },
        weight: 2,
    },
};
